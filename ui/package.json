{
  "name": "ui",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "merge": "^1.2.1",
    "pivotal-ui": "^17.0.2",
    "react": "^16.6.0",
    "react-dom": "^16.6.0",
    "react-scripts": "5.0.1",
    "timeago.js": "^3.0.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && rm -rf ../target/classes/static && mkdir -p ../target/classes/static && mv build/* ../target/classes/static/",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "proxy": "http://localhost:8080"
}
